# ── Web / API ────────────────────────────────────────────────────────────────
streamlit>=1.33
plotly>=5.20
fastapi>=0.110
uvicorn[standard]>=0.29
requests>=2.31

# ── Data / ML ────────────────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.24
lightgbm>=4.0          # demand, AOV, quantile and uplift models (needs libgomp1)
scikit-learn>=1.3      # cross-fitting and propensity diagnostics for uplift

# ── LLM layer (optional — chat + event ingestion) ────────────────────────────
# Only one is needed; pick the provider you have a key for.
anthropic>=0.39
openai>=1.40

# ── Utilities ────────────────────────────────────────────────────────────────
apscheduler>=3.10
openpyxl>=3.1
python-dateutil>=2.8
tzlocal>=5.0
pytz>=2024.1
